"""
pycheron.libs — Wrapped third-party library layer.

Access via:
    import pycheron as pycrn
    pycrn.pd.read_smart("data.csv")
    pycrn.np.normalize(arr)
    pycrn.sklearn.metrics.accuracy_score(y, y_pred)
"""
